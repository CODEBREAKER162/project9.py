# Method	           What it Does	Example
# append()	           Add item to end	fruits.append("grape")
# insert(index, x)	   Add item at specific place	fruits.insert(1, "orange")
# remove(x)	           Remove first item with value x	fruits.remove("banana")
# pop()            	   Removes last item	fruits.pop()
# pop(index)	       Removes item at specific position	fruits.pop(1)
# sort()	           Sort items (ascending by default)	marks.sort()
# reverse()	           Reverse the list	marks.reverse()
# index(x)	           Find index of item x	fruits.index("mango")
# count(x)	          Count how many times x appears	fruits.count("apple")
# clear()	          Removes all items — makes list empty	fruits.clear()


''' Strings:
Types are there in strings:
Access 1st letter ----->stringname[0]
Access last letter-------> string name[-1]
Length of the string---> len(stringname)
slice --->stringname[0:4]  # This will give you the first 4 characters
Concatenation ---> string1 + string2
Convert uppercase letters--->stringname.upper()
conver lowercase letter---->stringname.lower()
Replace text---->stringname.replace("old","new")
position find--->stringname.find(x)



'''



# my_set = {"apple", "banana", "cherry", "apple"}  # Sets automatically remove duplicates
# my_set.add("Orange")
# print(my_set)
'''| Method                | What it does                               |
| --------------------- | ------------------------------------------ |
| `.add(x)`             | Adds an item to the set                    |
| `.remove(x)`          | Removes item from set (error if not found) |
| `.discard(x)`         | Removes item safely (no error)             |
| `.clear()`            | Removes all items                          |
| `.union(set2)`        | Combines two sets (no duplicates)          |
| `.intersection(set2)` | Shows items that are in **both** sets      |
| `.difference(set2)`   | Shows items in first set but not in second |
| `.issubset(set2)`     | Checks if all items in first set are in second |
| `.issuperset(set2)`   | Checks if first set contains all items in second
'''
# a = {1, 2, 3}
# b = {3, 4, 5}
# c = a.union(b)-----> This will give you a new set with all unique items from both sets
# print(c)̥








# student ={
#     "Name": "Krishna",
#     "Marks": 100,
#     "Grade": "O"

# }
# print("Students marks",student["Marks"])----->This can acess directly to the users need from touple


# list of dictionary 



#import time
#time.sleep------>This is used for accordingly time of seconds reply 


